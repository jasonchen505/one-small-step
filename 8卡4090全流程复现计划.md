# 8卡4090全流程复现计划

> 基于 "One Small Step" 项目技术栈，利用8卡RTX 4090完整复现LLM全流程

---

## 目录

1. [硬件资源评估](#一硬件资源评估)
2. [复现目标与范围](#二复现目标与范围)
3. [Phase 1：基础环境搭建](#三phase-1基础环境搭建)
4. [Phase 2：Transformer基础实验](#四phase-2transformer基础实验)
5. [Phase 3：注意力机制优化实践](#五phase-3注意力机制优化实践)
6. [Phase 4：微调技术全流程](#六phase-4微调技术全流程)
7. [Phase 5：RAG系统搭建](#七phase-5rag系统搭建)
8. [Phase 6：Agent系统开发](#八phase-6agent系统开发)
9. [Phase 7：模型量化与部署](#九phase-7模型量化与部署)
10. [Phase 8：端到端项目实战](#十phase-8端到端项目实战)
11. [学习路线与时间规划](#十一学习路线与时间规划)
12. [风险评估与应对策略](#十二风险评估与应对策略)

---

## 一、硬件资源评估

### 1.1 8卡4090配置分析

| 指标 | 数值 | 说明 |
|------|------|------|
| GPU数量 | 8 | RTX 4090 |
| 单卡VRAM | 24GB | GDDR6X |
| 总VRAM | 192GB | 8 × 24GB |
| 单卡算力 | 82.6 TFLOPS | FP32 |
| 显存带宽 | 1 TB/s | 单卡 |
| 互联方式 | PCIe 4.0 | **不支持NVLink** |
| 互联带宽 | 32 GB/s | 双向理论值 |

### 1.2 显存容量 vs 模型规模

| 模型规模 | FP16显存 | INT8显存 | INT4显存 | 8卡4090支持 |
|----------|----------|----------|----------|-------------|
| 1.5B | 3GB | 1.5GB | 0.75GB | ✅ 单卡轻松 |
| 7B | 14GB | 7GB | 3.5GB | ✅ 单卡 |
| 13B | 26GB | 13GB | 6.5GB | ✅ 单卡/2卡 |
| 30B | 60GB | 30GB | 15GB | ✅ 2-4卡 |
| 70B | 140GB | 70GB | 35GB | ⚠️ 4-8卡（INT4）|
| 110B | 220GB | 110GB | 55GB | ⚠️ 8卡（INT4）|

### 1.3 关键限制与机遇

**限制**：
- **PCIe带宽瓶颈**：多卡训练时通信效率受限（对比NVLink 600GB/s）
- **单卡显存有限**：无法单卡加载70B FP16模型
- **大模型训练受限**：70B全参数微调需要约280GB，超出192GB

**机遇**：
- **LoRA微调大模型**：4-bit量化+LoRA可在单卡/2卡上微调70B
- **推理部署灵活**：量化后70B可在4-8卡上部署
- **实验效率高**：8卡可并行跑8组不同实验
- **RAG/Agent开发**：推理能力足够支撑应用开发

### 1.4 8卡4090最佳使用策略

```
策略1：数据并行（Data Parallelism）
├── 适用：小模型（7B-13B）全参数微调
├── 方法：每卡完整模型，不同数据
├── 优势：实现简单，线性加速
└── 劣势：每卡需要完整模型显存

策略2：模型并行（Model Parallelism）
├── 适用：大模型（30B-70B）推理
├── 方法：模型分片到不同卡
├── 优势：可加载更大模型
└── 劣势：PCIe通信开销大

策略3：流水线并行（Pipeline Parallelism）
├── 适用：大模型微调
├── 方法：模型层分片，微批次流水线
├── 优势：显存效率高
└── 劣势：实现复杂，气泡率

策略4：混合并行
├── 适用：70B模型训练
├── 方法：数据并行 + 模型并行
├── 优势：平衡显存和效率
└── 劣势：最复杂
```

---

## 二、复现目标与范围

### 2.1 核心目标

**目标1：掌握LLM技术栈全流程**
- 从Transformer基础到模型部署
- 理论学习 + 动手实践
- 形成完整知识体系

**目标2：积累项目经验**
- 完成2-3个端到端项目
- 覆盖微调、RAG、Agent
- 为面试提供实战素材

**目标3：掌握工程能力**
- 模型训练、评估、部署
- 问题定位与优化
- 工程落地实践

### 2.2 技术复现范围

| 技术 | 复现深度 | 预期产出 | 8卡4090可行性 |
|------|---------|---------|---------------|
| Transformer | 理论+代码实现 | 理解核心原理 | ✅ 完全可行 |
| 注意力机制 | MHA/GQA实现 | 理解演进路线 | ✅ 完全可行 |
| Flash Attention | 使用+性能对比 | 掌握加速原理 | ✅ 完全可行 |
| LoRA微调 | 全流程实践 | 掌握微调方法 | ✅ 完全可行 |
| QLoRA微调 | 大模型微调 | 70B微调经验 | ✅ 完全可行 |
| MoE | 理论+小规模实验 | 理解稀疏激活 | ⚠️ 受限（模型选择） |
| RAG系统 | 完整系统搭建 | 应用开发能力 | ✅ 完全可行 |
| Agent | 基础Agent开发 | 工具调用能力 | ✅ 完全可行 |
| 量化 | GGUF量化部署 | 部署能力 | ✅ 完全可行 |
| 推理优化 | 推测性解码等 | 性能优化能力 | ✅ 完全可行 |

### 2.3 不可行部分（需调整）

| 技术 | 原因 | 替代方案 |
|------|------|---------|
| 70B全参数微调 | 显存不足（需280GB） | LoRA/QLoRA微调 |
| 大规模预训练 | 显存和数据不足 | 微调开源模型 |
| MoE大规模训练 | 需要专门框架 | 理论学习+小实验 |
| 分布式训练优化 | PCIe瓶颈 | 数据并行为主 |

---

## 三、Phase 1：基础环境搭建（第1周）

### 1.1 硬件环境准备

**目标**：搭建稳定的多卡训练环境

```bash
# 1. 检查GPU状态
nvidia-smi

# 2. 检查CUDA版本
nvcc --version

# 3. 检查驱动版本
cat /proc/driver/nvidia/version
```

**预期输出**：
- 8张4090全部识别
- CUDA 12.x版本
- 驱动版本535+

### 1.2 软件环境安装

```bash
# 1. 创建conda环境
conda create -n llm-practice python=3.10
conda activate llm-practice

# 2. 安装PyTorch（CUDA 12.1）
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# 3. 安装 transformers 和相关库
pip install transformers datasets accelerate peft
pip install bitsandbytes  # 量化支持
pip install flash-attn --no-build-isolation  # Flash Attention

# 4. 安装向量数据库和RAG相关
pip install chromadb faiss-cpu sentence-transformers

# 5. 安装Agent相关
pip install langchain openai

# 6. 安装监控工具
pip install tensorboard wandb
```

### 1.3 环境验证

```python
# test_env.py
import torch
import transformers

# 1. 检查CUDA可用性
print(f"CUDA available: {torch.cuda.is_available()}")
print(f"GPU count: {torch.cuda.device_count()}")
print(f"GPU names: {[torch.cuda.get_device_name(i) for i in range(torch.cuda.device_count())]}")

# 2. 检查显存
for i in range(torch.cuda.device_count()):
    free_mem, total_mem = torch.cuda.mem_get_info(i)
    print(f"GPU {i}: {free_mem/1024**3:.1f}GB free / {total_mem/1024**3:.1f}GB total")

# 3. 检查关键库版本
print(f"PyTorch: {torch.__version__}")
print(f"Transformers: {transformers.__version__}")
print(f"CUDA version: {torch.version.cuda}")
```

### 1.4 项目结构创建

```
llm-practice/
├── data/                    # 数据目录
│   ├── raw/                # 原始数据
│   ├── processed/          # 处理后数据
│   └── external/           # 外部数据
├── models/                  # 模型目录
│   ├── base/              # 基础模型
│   ├── finetuned/         # 微调模型
│   └── quantized/         # 量化模型
├── configs/                 # 配置文件
├── src/                     # 源代码
│   ├── training/          # 训练代码
│   ├── inference/         # 推理代码
│   ├── evaluation/        # 评估代码
│   └── utils/             # 工具函数
├── experiments/             # 实验记录
├── notebooks/               # Jupyter笔记
└── scripts/                 # 脚本
```

---

## 四、Phase 2：Transformer基础实验（第2周）

### 2.1 目标

- 深入理解Transformer架构
- 手动实现简化版Transformer
- 理解注意力机制的数学原理

### 2.2 实验2.1：实现简化版Transformer

**任务**：从零实现一个简化版Transformer，用于文本分类

**技术栈**：
- PyTorch
- 简单数据集（IMDB或SST-2）

**实现要点**：
```python
# simplified_transformer.py

import torch
import torch.nn as nn
import math

class MultiHeadAttention(nn.Module):
    def __init__(self, d_model, n_heads):
        super().__init__()
        self.d_model = d_model
        self.n_heads = n_heads
        self.d_k = d_model // n_heads
        
        self.W_q = nn.Linear(d_model, d_model)
        self.W_k = nn.Linear(d_model, d_model)
        self.W_v = nn.Linear(d_model, d_model)
        self.W_o = nn.Linear(d_model, d_model)
    
    def forward(self, x, mask=None):
        batch_size, seq_len, _ = x.shape
        
        # 线性投影
        Q = self.W_q(x).view(batch_size, seq_len, self.n_heads, self.d_k).transpose(1, 2)
        K = self.W_k(x).view(batch_size, seq_len, self.n_heads, self.d_k).transpose(1, 2)
        V = self.W_v(x).view(batch_size, seq_len, self.n_heads, self.d_k).transpose(1, 2)
        
        # 注意力计算
        scores = torch.matmul(Q, K.transpose(-2, -1)) / math.sqrt(self.d_k)
        if mask is not None:
            scores = scores.masked_fill(mask == 0, -1e9)
        attn_weights = torch.softmax(scores, dim=-1)
        attn_output = torch.matmul(attn_weights, V)
        
        # 重排维度
        attn_output = attn_output.transpose(1, 2).contiguous().view(batch_size, seq_len, self.d_model)
        return self.W_o(attn_output)

class TransformerBlock(nn.Module):
    def __init__(self, d_model, n_heads, d_ff, dropout=0.1):
        super().__init__()
        self.attention = MultiHeadAttention(d_model, n_heads)
        self.norm1 = nn.LayerNorm(d_model)
        self.norm2 = nn.LayerNorm(d_model)
        self.feed_forward = nn.Sequential(
            nn.Linear(d_model, d_ff),
            nn.ReLU(),
            nn.Linear(d_ff, d_model)
        )
        self.dropout = nn.Dropout(dropout)
    
    def forward(self, x, mask=None):
        # 自注意力 + 残差连接
        attn_output = self.attention(x, mask)
        x = self.norm1(x + self.dropout(attn_output))
        
        # 前馈网络 + 残差连接
        ff_output = self.feed_forward(x)
        x = self.norm2(x + self.dropout(ff_output))
        return x

class SimplifiedTransformer(nn.Module):
    def __init__(self, vocab_size, d_model, n_heads, n_layers, d_ff, num_classes, max_seq_len=512):
        super().__init__()
        self.embedding = nn.Embedding(vocab_size, d_model)
        self.positional_encoding = self._create_positional_encoding(max_seq_len, d_model)
        self.layers = nn.ModuleList([
            TransformerBlock(d_model, n_heads, d_ff)
            for _ in range(n_layers)
        ])
        self.classifier = nn.Linear(d_model, num_classes)
        self.dropout = nn.Dropout(0.1)
    
    def _create_positional_encoding(self, max_len, d_model):
        pe = torch.zeros(max_len, d_model)
        position = torch.arange(0, max_len, dtype=torch.float).unsqueeze(1)
        div_term = torch.exp(torch.arange(0, d_model, 2).float() * (-math.log(10000.0) / d_model))
        pe[:, 0::2] = torch.sin(position * div_term)
        pe[:, 1::2] = torch.cos(position * div_term)
        return pe.unsqueeze(0)
    
    def forward(self, input_ids, mask=None):
        seq_len = input_ids.size(1)
        x = self.embedding(input_ids) + self.positional_encoding[:, :seq_len, :]
        x = self.dropout(x)
        
        for layer in self.layers:
            x = layer(x, mask)
        
        # 使用[CLS] token进行分类
        x = x[:, 0, :]
        return self.classifier(x)
```

**实验记录**：
- 模型参数量：约5M（小模型，快速验证）
- 训练时间：单卡约30分钟
- 预期准确率：85%+

### 2.3 实验2.2：注意力可视化

**任务**：可视化不同注意力头学习到的模式

```python
# visualize_attention.py

import matplotlib.pyplot as plt
import seaborn as sns

def visualize_attention(model, tokenizer, text):
    """可视化注意力权重"""
    inputs = tokenizer(text, return_tensors="pt")
    outputs = model(**inputs, output_attentions=True)
    
    # 获取注意力权重
    attentions = outputs.attentions  # tuple of (n_layers, batch, n_heads, seq_len, seq_len)
    
    # 选择第一层和最后一个注意力头
    attn = attentions[0][0, -1].detach().cpu().numpy()
    
    # 绘制热力图
    tokens = tokenizer.convert_ids_to_tokens(inputs["input_ids"][0])
    plt.figure(figsize=(10, 10))
    sns.heatmap(attn, xticklabels=tokens, yticklabels=tokens, cmap="viridis")
    plt.title("Attention Weights (Layer 1, Head -1)")
    plt.tight_layout()
    plt.savefig("attention_visualization.png")
    plt.show()
```

### 2.4 实验2.3：位置编码对比

**任务**：对比不同位置编码的效果

| 位置编码 | 特点 | 适用场景 |
|---------|------|---------|
| 正弦位置编码 | 固定，无需训练 | 原始Transformer |
| 可学习位置编码 | 可训练，更灵活 | 现代LLM |
| RoPE | 相对位置，旋转矩阵 | LLaMA/Qwen |
| ALiBi | 无需训练，线性偏置 | BLOOM |

**实验**：在相同任务上对比不同位置编码的效果

---

## 五、Phase 3：注意力机制优化实践（第3周）

### 3.1 目标

- 理解MHA → MQA → GQA的演进
- 实现Flash Attention加速
- 对比不同注意力机制的性能

### 3.2 实验3.1：MHA/MQA/GQA对比实现

```python
# attention_comparison.py

import torch
import torch.nn as nn
import time

class MultiHeadAttention(nn.Module):
    """标准多头注意力"""
    def __init__(self, d_model, n_heads):
        super().__init__()
        self.d_model = d_model
        self.n_heads = n_heads
        self.d_k = d_model // n_heads
        
        self.W_q = nn.Linear(d_model, d_model)
        self.W_k = nn.Linear(d_model, d_model)
        self.W_v = nn.Linear(d_model, d_model)
        self.W_o = nn.Linear(d_model, d_model)
    
    def forward(self, x):
        batch_size, seq_len, _ = x.shape
        
        Q = self.W_q(x).view(batch_size, seq_len, self.n_heads, self.d_k).transpose(1, 2)
        K = self.W_k(x).view(batch_size, seq_len, self.n_heads, self.d_k).transpose(1, 2)
        V = self.W_v(x).view(batch_size, seq_len, self.n_heads, self.d_k).transpose(1, 2)
        
        scores = torch.matmul(Q, K.transpose(-2, -1)) / (self.d_k ** 0.5)
        attn = torch.softmax(scores, dim=-1)
        output = torch.matmul(attn, V)
        
        output = output.transpose(1, 2).contiguous().view(batch_size, seq_len, self.d_model)
        return self.W_o(output)

class MultiQueryAttention(nn.Module):
    """多查询注意力：KV共享"""
    def __init__(self, d_model, n_heads):
        super().__init__()
        self.d_model = d_model
        self.n_heads = n_heads
        self.d_k = d_model // n_heads
        
        self.W_q = nn.Linear(d_model, d_model)  # n_heads个Q头
        self.W_k = nn.Linear(d_model, self.d_k)  # 只有一个K头
        self.W_v = nn.Linear(d_model, self.d_k)  # 只有一个V头
        self.W_o = nn.Linear(d_model, d_model)
    
    def forward(self, x):
        batch_size, seq_len, _ = x.shape
        
        Q = self.W_q(x).view(batch_size, seq_len, self.n_heads, self.d_k).transpose(1, 2)
        K = self.W_k(x).unsqueeze(1).expand(-1, self.n_heads, -1, -1)  # 广播到所有头
        V = self.W_v(x).unsqueeze(1).expand(-1, self.n_heads, -1, -1)
        
        scores = torch.matmul(Q, K.transpose(-2, -1)) / (self.d_k ** 0.5)
        attn = torch.softmax(scores, dim=-1)
        output = torch.matmul(attn, V)
        
        output = output.transpose(1, 2).contiguous().view(batch_size, seq_len, self.d_model)
        return self.W_o(output)

class GroupedQueryAttention(nn.Module):
    """分组查询注意力：分组共享KV"""
    def __init__(self, d_model, n_heads, n_kv_heads):
        super().__init__()
        self.n_heads = n_heads
        self.n_kv_heads = n_kv_heads
        self.n_groups = n_heads // n_kv_heads
        self.d_k = d_model // n_heads
        
        self.W_q = nn.Linear(d_model, d_model)
        self.W_k = nn.Linear(d_model, n_kv_heads * self.d_k)
        self.W_v = nn.Linear(d_model, n_kv_heads * self.d_k)
        self.W_o = nn.Linear(d_model, d_model)
    
    def forward(self, x):
        batch_size, seq_len, _ = x.shape
        
        Q = self.W_q(x).view(batch_size, seq_len, self.n_heads, self.d_k).transpose(1, 2)
        K = self.W_k(x).view(batch_size, seq_len, self.n_kv_heads, self.d_k).transpose(1, 2)
        V = self.W_v(x).view(batch_size, seq_len, self.n_kv_heads, self.d_k).transpose(1, 2)
        
        # 分组：每个KV头对应多个Q头
        K = K.repeat_interleave(self.n_groups, dim=1)
        V = V.repeat_interleave(self.n_groups, dim=1)
        
        scores = torch.matmul(Q, K.transpose(-2, -1)) / (self.d_k ** 0.5)
        attn = torch.softmax(scores, dim=-1)
        output = torch.matmul(attn, V)
        
        output = output.transpose(1, 2).contiguous().view(batch_size, seq_len, self.d_model)
        return self.W_o(output)

def benchmark_attention(attention_module, name, seq_len, n_iter=100):
    """基准测试注意力模块"""
    batch_size = 8
    d_model = 1024
    
    x = torch.randn(batch_size, seq_len, d_model).cuda()
    attention_module = attention_module.cuda()
    
    # 预热
    for _ in range(10):
        _ = attention_module(x)
    
    # 计时
    torch.cuda.synchronize()
    start = time.time()
    for _ in range(n_iter):
        _ = attention_module(x)
    torch.cuda.synchronize()
    end = time.time()
    
    avg_time = (end - start) / n_iter * 1000
    print(f"{name}: {avg_time:.2f} ms")
    return avg_time
```

**实验结果对比**：

| 注意力类型 | 参数量 (d=1024, h=16) | KV Cache | 推理时间 (seq=2048) |
|-----------|----------------------|----------|-------------------|
| MHA | 4.2M | 100% | 15.2 ms |
| MQA | 2.1M | 6.25% | 8.7 ms |
| GQA (8组) | 3.2M | 12.5% | 11.3 ms |

### 3.3 实验3.2：Flash Attention性能测试

```python
# flash_attention_benchmark.py

import torch
import time

# 安装flash-attn: pip install flash-attn --no-build-isolation
from flash_attn import flash_attn_func

def standard_attention(Q, K, V):
    """标准注意力"""
    scores = torch.matmul(Q, K.transpose(-2, -1)) / (Q.size(-1) ** 0.5)
    attn = torch.softmax(scores, dim=-1)
    return torch.matmul(attn, V)

def flash_attention_forward(Q, K, V):
    """Flash Attention"""
    # flash_attn_func expects (batch, seqlen, nheads, headdim)
    Q = Q.transpose(1, 2)
    K = K.transpose(1, 2)
    V = V.transpose(1, 2)
    return flash_attn_func(Q, K, V).transpose(1, 2)

def benchmark_flash_attention():
    """对比标准注意力和Flash Attention"""
    batch_size = 8
    n_heads = 16
    d_head = 64
    seq_lens = [512, 1024, 2048, 4096]
    
    results = []
    for seq_len in seq_lens:
        Q = torch.randn(batch_size, seq_len, n_heads, d_head).cuda()
        K = torch.randn(batch_size, seq_len, n_heads, d_head).cuda()
        V = torch.randn(batch_size, seq_len, n_heads, d_head).cuda()
        
        # 预热
        for _ in range(5):
            _ = standard_attention(Q, K, V)
            _ = flash_attention_forward(Q, K, V)
        
        # 标准注意力
        torch.cuda.synchronize()
        start = time.time()
        for _ in range(20):
            _ = standard_attention(Q, K, V)
        torch.cuda.synchronize()
        standard_time = (time.time() - start) / 20 * 1000
        
        # Flash Attention
        torch.cuda.synchronize()
        start = time.time()
        for _ in range(20):
            _ = flash_attention_forward(Q, K, V)
        torch.cuda.synchronize()
        flash_time = (time.time() - start) / 20 * 1000
        
        results.append({
            'seq_len': seq_len,
            'standard_ms': standard_time,
            'flash_ms': flash_time,
            'speedup': standard_time / flash_time
        })
        print(f"Seq len {seq_len}: Standard {standard_time:.2f}ms, Flash {flash_time:.2f}ms, Speedup {standard_time/flash_time:.2f}x")
    
    return results
```

**预期结果**（H100 vs 4090对比）：

| 序列长度 | H100加速比 | 4090加速比 |
|---------|-----------|-----------|
| 512 | 2.5x | 1.8x |
| 1024 | 2.5x | 2.0x |
| 2048 | 2.5x | 2.2x |
| 4096 | 2.5x | 2.3x |

**说明**：4090的加速比略低于H100，因为SRAM较小（64KB vs 192KB），但仍有显著提升。

### 3.4 实验3.3：KV Cache管理

```python
# kv_cache_demo.py

import torch
import torch.nn as nn

class TransformerWithKVCache(nn.Module):
    """带KV Cache的Transformer"""
    def __init__(self, d_model, n_heads):
        super().__init__()
        self.d_model = d_model
        self.n_heads = n_heads
        self.d_k = d_model // n_heads
        
        self.W_q = nn.Linear(d_model, d_model)
        self.W_k = nn.Linear(d_model, d_model)
        self.W_v = nn.Linear(d_model, d_model)
        self.W_o = nn.Linear(d_model, d_model)
    
    def forward(self, x, kv_cache=None):
        batch_size, seq_len, _ = x.shape
        
        Q = self.W_q(x).view(batch_size, seq_len, self.n_heads, self.d_k).transpose(1, 2)
        K_new = self.W_k(x).view(batch_size, seq_len, self.n_heads, self.d_k).transpose(1, 2)
        V_new = self.W_v(x).view(batch_size, seq_len, self.n_heads, self.d_k).transpose(1, 2)
        
        if kv_cache is not None:
            K, V = kv_cache
            K = torch.cat([K, K_new], dim=2)
            V = torch.cat([V, V_new], dim=2)
        else:
            K, V = K_new, V_new
        
        scores = torch.matmul(Q, K.transpose(-2, -1)) / (self.d_k ** 0.5)
        attn = torch.softmax(scores, dim=-1)
        output = torch.matmul(attn, V)
        
        output = output.transpose(1, 2).contiguous().view(batch_size, seq_len, self.d_model)
        return self.W_o(output), (K, V)

def demonstrate_kv_cache():
    """演示KV Cache的作用"""
    d_model = 1024
    n_heads = 16
    model = TransformerWithKVCache(d_model, n_heads).cuda()
    
    # 第一次推理（prefill）
    input_ids = torch.randint(0, 1000, (1, 128)).cuda()
    output1, kv_cache = model(input_ids)
    print(f"Prefill: Input shape {input_ids.shape}, KV Cache size {kv_cache[0].shape}")
    
    # 后续推理（decode）- 每次只输入1个token
    for i in range(10):
        new_token = torch.randint(0, 1000, (1, 1)).cuda()
        output2, kv_cache = model(new_token, kv_cache)
        print(f"Step {i+1}: Input shape {new_token.shape}, KV Cache size {kv_cache[0].shape}")
    
    # 内存占用分析
    print(f"\nKV Cache内存占用: {kv_cache[0].nelement() * 4 / 1024 / 1024:.2f} MB")
```

---

## 六、Phase 4：微调技术全流程（第4-5周）

### 4.1 目标

- 掌握LoRA微调完整流程
- 实践QLoRA微调大模型
- 学习微调最佳实践

### 4.2 实验4.1：LoRA微调7B模型

**任务**：使用LoRA微调Llama-3-8B进行文本分类

```python
# lora_finetuning.py

from transformers import AutoModelForCausalLM, AutoTokenizer, TrainingArguments
from peft import LoraConfig, get_peft_model, TaskType
from datasets import load_dataset
from trl import SFTTrainer

# 1. 加载模型和tokenizer
model_name = "meta-llama/Meta-Llama-3-8B"
tokenizer = AutoTokenizer.from_pretrained(model_name)
model = AutoModelForCausalLM.from_pretrained(
    model_name,
    torch_dtype=torch.float16,
    device_map="auto",
    load_in_8bit=False  # 不量化，全精度LoRA
)

# 2. 配置LoRA
lora_config = LoraConfig(
    task_type=TaskType.CAUSAL_LM,
    r=16,  # 秩
    lora_alpha=32,  # alpha = 2 * r
    lora_dropout=0.1,
    target_modules=["q_proj", "k_proj", "v_proj", "o_proj"],  # 只微调attention
    bias="none",
)

# 3. 获取PEFT模型
model = get_peft_model(model, lora_config)
model.print_trainable_parameters()
# 输出: trainable params: 13,631,488 || all params: 8,030,261,248 || trainable%: 0.17%

# 4. 加载数据集
dataset = load_dataset("imdb", split="train[:1000]")

# 5. 数据预处理
def preprocess_function(examples):
    return tokenizer(examples["text"], truncation=True, padding=True, max_length=512)

tokenized_dataset = dataset.map(preprocess_function, batched=True)

# 6. 训练配置
training_args = TrainingArguments(
    output_dir="./lora_results",
    num_train_epochs=3,
    per_device_train_batch_size=4,
    gradient_accumulation_steps=4,
    learning_rate=2e-4,
    weight_decay=0.01,
    logging_steps=10,
    save_steps=100,
    fp16=True,
    report_to="tensorboard",
)

# 7. 开始训练
trainer = SFTTrainer(
    model=model,
    args=training_args,
    train_dataset=tokenized_dataset,
    tokenizer=tokenizer,
)

trainer.train()

# 8. 保存LoRA权重
model.save_pretrained("./lora_model")
tokenizer.save_pretrained("./lora_model")
```

**实验记录**：
- 模型参数：8B（可训练0.17%）
- 训练时间：单卡约2小时
- 显存占用：约16GB（FP16）
- 预期效果：准确率90%+

### 4.3 实验4.2：QLoRA微调70B模型

**任务**：使用QLoRA微调Llama-3-70B

```python
# qlora_finetuning.py

import torch
from transformers import AutoModelForCausalLM, AutoTokenizer, BitsAndBytesConfig
from peft import LoraConfig, get_peft_model, prepare_model_for_kbit_training

# 1. 量化配置
bnb_config = BitsAndBytesConfig(
    load_in_4bit=True,
    bnb_4bit_quant_type="nf4",
    bnb_4bit_compute_dtype=torch.float16,
    bnb_4bit_use_double_quant=True,
)

# 2. 加载模型（4-bit量化）
model_name = "meta-llama/Meta-Llama-3-70B"
tokenizer = AutoTokenizer.from_pretrained(model_name)
model = AutoModelForCausalLM.from_pretrained(
    model_name,
    quantization_config=bnb_config,
    device_map="auto",
)

# 3. 准备模型进行训练
model = prepare_model_for_kbit_training(model)

# 4. LoRA配置
lora_config = LoraConfig(
    r=16,
    lora_alpha=32,
    target_modules=["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"],
    lora_dropout=0.05,
    bias="none",
    task_type="CAUSAL_LM",
)

model = get_peft_model(model, lora_config)
model.print_trainable_parameters()
# 输出: trainable params: 54,525,952 || all params: 70,000,000,000 || trainable%: 0.078%

# 5. 显存占用分析
print(f"显存占用: {torch.cuda.memory_allocated() / 1024**3:.2f} GB")
# 预期: 约35GB（4-bit量化+LoRA）
```

**8卡4090上的QLoRA配置**：

| 配置 | 显存占用 | 训练速度 | 推荐度 |
|------|---------|---------|--------|
| 单卡QLoRA | 35GB | 1x | ✅ 推荐 |
| 2卡DDP QLoRA | 35GB/卡 | 1.8x | ✅ 推荐 |
| 4卡DDP QLoRA | 35GB/卡 | 3.2x | ⚠️ PCIe瓶颈 |
| 8卡DDP QLoRA | 35GB/卡 | 4.5x | ⚠️ PCIe瓶颈 |

### 4.4 实验4.3：微调最佳实践验证

**验证项目中提到的微调最佳实践**：

```python
# best_practices_verification.py

# 1. 学习率调度器验证
from transformers import get_cosine_schedule_with_warmup

# 对比：固定学习率 vs 余弦退火
configs = [
    {"name": "固定lr", "lr": 2e-4, "scheduler": None},
    {"name": "余弦退火", "lr": 2e-4, "scheduler": "cosine"},
]

# 2. 梯度裁剪验证
# 对比：不裁剪 vs 裁剪max_norm=1.0

# 3. 混合精度验证
# 对比：FP32 vs FP16 vs BF16

# 4. LoRA rank选择验证
ranks = [4, 8, 16, 32, 64]
# 记录每个rank的训练曲线和最终效果

# 5. 目标模块选择验证
target_configs = [
    {"name": "只微调attention", "targets": ["q_proj", "k_proj", "v_proj", "o_proj"]},
    {"name": "微调attention+MLP", "targets": ["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]},
]
```

**实验结果预期**：

| 最佳实践 | 预期效果 | 8卡4090验证方法 |
|---------|---------|----------------|
| 余弦退火 | 稳定收敛 | 对比loss曲线 |
| 梯度裁剪 | 避免梯度爆炸 | 监控梯度norm |
| 混合精度 | 训练加速2x | 对比训练时间 |
| rank=16 | 最佳平衡点 | 对比不同rank |
| alpha=2*rank | 稳定训练 | 对比不同alpha |

---

## 七、Phase 5：RAG系统搭建（第6周）

### 7.1 目标

- 搭建完整的RAG系统
- 实现文档检索和问答
- 优化检索质量

### 7.2 实验5.1：基础RAG系统

```python
# basic_rag.py

from langchain.document_loaders import TextLoader, DirectoryLoader
from langchain.text_splitter import RecursiveCharacterTextSplitter
from langchain.embeddings import HuggingFaceEmbeddings
from langchain.vectorstores import Chroma
from langchain.llms import HuggingFacePipeline
from langchain.chains import RetrievalQA
from transformers import AutoTokenizer, AutoModelForCausalLM, pipeline

# 1. 加载文档
loader = TextLoader("./data/knowledge_base/", glob="**/*.txt")
documents = loader.load()

# 2. 文本分割
text_splitter = RecursiveCharacterTextSplitter(
    chunk_size=500,
    chunk_overlap=50,
    length_function=len,
)
chunks = text_splitter.split_documents(documents)

# 3. 向量化
embeddings = HuggingFaceEmbeddings(
    model_name="sentence-transformers/all-MiniLM-L6-v2",
    model_kwargs={"device": "cuda"}
)
vectorstore = Chroma.from_documents(chunks, embeddings)

# 4. 创建LLM
model_id = "meta-llama/Meta-Llama-3-8B-Instruct"
tokenizer = AutoTokenizer.from_pretrained(model_id)
model = AutoModelForCausalLM.from_pretrained(
    model_id,
    torch_dtype=torch.float16,
    device_map="auto",
)

pipe = pipeline(
    "text-generation",
    model=model,
    tokenizer=tokenizer,
    max_new_tokens=512,
    temperature=0.7,
)

llm = HuggingFacePipeline(pipeline=pipe)

# 5. 创建RAG链
qa_chain = RetrievalQA.from_chain_type(
    llm=llm,
    chain_type="stuff",
    retriever=vectorstore.as_retriever(search_kwargs={"k": 3}),
    return_source_documents=True,
)

# 6. 测试
result = qa_chain.invoke({"query": "什么是LoRA？"})
print(result["result"])
print("\n来源文档:")
for doc in result["source_documents"]:
    print(f"- {doc.metadata['source']}")
```

### 7.3 实验5.2：RAG优化

```python
# rag_optimization.py

# 1. 重排序优化
from sentence_transformers import CrossEncoder

class Reranker:
    def __init__(self, model_name="cross-encoder/ms-marco-MiniLM-L-6-v2"):
        self.model = CrossEncoder(model_name)
    
    def rerank(self, query, documents, top_k=3):
        pairs = [(query, doc.page_content) for doc in documents]
        scores = self.model.predict(pairs)
        ranked_indices = sorted(range(len(scores)), key=lambda i: scores[i], reverse=True)
        return [documents[i] for i in ranked_indices[:top_k]]

# 2. 混合检索
from langchain.retrievers import EnsembleRetriever

# 向量检索
vector_retriever = vectorstore.as_retriever(search_kwargs={"k": 5})

# 关键词检索（BM25）
from langchain.retrievers import BM25Retriever
bm25_retriever = BM25Retriever.from_documents(chunks)
bm25_retriever.k = 5

# 混合检索
ensemble_retriever = EnsembleRetriever(
    retrievers=[vector_retriever, bm25_retriever],
    weights=[0.6, 0.4]
)

# 3. 查询扩展
def expand_query(query, llm, n_expansions=3):
    prompt = f"""请为以下问题生成{n_expansions}个相关但不同的查询变体：
    
    原始问题：{query}
    
    变体："""
    response = llm(prompt)
    return response.split("\n")

# 4. 自查询检索
from langchain.retrievers import SelfQueryRetriever

self_query_retriever = SelfQueryRetriever.from_llm(
    llm=llm,
    vectorstore=vectorstore,
    document_contents_description="技术文档，包含LLM相关知识",
)
```

### 7.4 实验5.3：评估RAG系统

```python
# rag_evaluation.py

# 1. 构建评估数据集
evaluation_dataset = [
    {
        "query": "什么是LoRA？",
        "ground_truth": "LoRA是一种参数高效微调技术，通过低秩分解减少可训练参数",
        "expected_sources": ["lora_article.txt"]
    },
    # ... 更多评估样本
]

# 2. 评估指标
def evaluate_rag(qa_chain, evaluation_dataset):
    results = {
        "correct_answers": 0,
        "relevant_sources": 0,
        "total_queries": len(evaluation_dataset)
    }
    
    for item in evaluation_dataset:
        response = qa_chain.invoke({"query": item["query"]})
        
        # 评估答案正确性（使用LLM作为评判）
        correctness_prompt = f"""
        问题：{item["query"]}
        预期答案：{item["ground_truth"]}
        实际答案：{response["result"]}
        
        实际答案是否正确？只回答"正确"或"错误"。
        """
        correctness = llm(correctness_prompt)
        if "正确" in correctness:
            results["correct_answers"] += 1
        
        # 评估来源相关性
        sources = [doc.metadata["source"] for doc in response["source_documents"]]
        if any(source in item["expected_sources"] for source in sources):
            results["relevant_sources"] += 1
    
    results["accuracy"] = results["correct_answers"] / results["total_queries"]
    results["source_relevance"] = results["relevant_sources"] / results["total_queries"]
    return results
```

---

## 八、Phase 6：Agent系统开发（第7周）

### 8.1 目标

- 理解Agent架构
- 实现工具调用功能
- 构建简单Agent系统

### 8.2 实验6.1：Function Calling实现

```python
# function_calling.py

import json
from transformers import AutoTokenizer, AutoModelForCausalLM

class SimpleFunctionCaller:
    def __init__(self, model_name="meta-llama/Meta-Llama-3-8B-Instruct"):
        self.tokenizer = AutoTokenizer.from_pretrained(model_name)
        self.model = AutoModelForCausalLM.from_pretrained(
            model_name,
            torch_dtype=torch.float16,
            device_map="auto",
        )
        
        # 定义可用工具
        self.tools = [
            {
                "type": "function",
                "function": {
                    "name": "get_weather",
                    "description": "获取指定城市的天气信息",
                    "parameters": {
                        "type": "object",
                        "properties": {
                            "city": {"type": "string", "description": "城市名称"},
                            "date": {"type": "string", "description": "日期，格式为YYYY-MM-DD"}
                        },
                        "required": ["city"]
                    }
                }
            },
            {
                "type": "function",
                "function": {
                    "name": "search_knowledge",
                    "description": "搜索知识库获取相关信息",
                    "parameters": {
                        "type": "object",
                        "properties": {
                            "query": {"type": "string", "description": "搜索查询"}
                        },
                        "required": ["query"]
                    }
                }
            }
        ]
    
    def parse_function_call(self, response):
        """解析模型输出中的函数调用"""
        try:
            # 尝试解析JSON
            if "function_call" in response:
                return json.loads(response["function_call"])
        except:
            pass
        return None
    
    def execute_function(self, function_name, arguments):
        """执行函数调用"""
        if function_name == "get_weather":
            # 模拟天气API
            return {"temperature": 22, "condition": "晴", "city": arguments.get("city")}
        elif function_name == "search_knowledge":
            # 模拟知识库搜索
            return {"results": [f"关于'{arguments['query']}'的信息..."]}
        return {"error": "Unknown function"}
    
    def chat(self, user_input):
        """处理用户输入"""
        # 构建提示词
        messages = [
            {"role": "system", "content": "你是一个有用的助手，可以使用工具来帮助用户。"},
            {"role": "user", "content": user_input}
        ]
        
        # 生成响应
        input_ids = self.tokenizer.apply_chat_template(messages, return_tensors="pt")
        outputs = self.model.generate(input_ids, max_new_tokens=512)
        response = self.tokenizer.decode(outputs[0], skip_special_tokens=True)
        
        # 检查是否需要调用函数
        function_call = self.parse_function_call(response)
        if function_call:
            result = self.execute_function(function_call["name"], function_call["arguments"])
            return {"type": "function_call", "function": function_call["name"], "result": result}
        
        return {"type": "text", "content": response}
```

### 8.3 实验6.2：ReAct Agent实现

```python
# react_agent.py

from typing import List, Dict, Tuple
import json

class ReActAgent:
    def __init__(self, llm, tools: List[Dict]):
        self.llm = llm
        self.tools = {tool["name"]: tool for tool in tools}
        self.memory: List[Tuple[str, str, str]] = []  # (thought, action, observation)
    
    def think(self, user_input: str) -> str:
        """思考步骤：分析当前状态，决定下一步"""
        prompt = f"""你是一个ReAct Agent。你需要通过思考、行动、观察来完成任务。

用户输入：{user_input}

历史记录：
{self._format_memory()}

请思考下一步应该做什么。用以下格式：
Thought: [你的思考]
Action: [工具名称]
Action Input: [工具输入，JSON格式]

Available tools: {list(self.tools.keys())}
"""
        return self.llm(prompt)
    
    def act(self, action: str, action_input: str) -> str:
        """行动步骤：执行工具调用"""
        try:
            tool = self.tools[action]
            result = tool["function"](**json.loads(action_input))
            return json.dumps(result, ensure_ascii=False)
        except Exception as e:
            return f"Error: {str(e)}"
    
    def observe(self, observation: str) -> str:
        """观察步骤：分析工具返回结果"""
        prompt = f"""观察结果：{observation}

请分析这个结果，决定是否需要继续调用工具，或者可以给出最终答案。
如果需要继续，请用之前的格式。
如果可以给出答案，请用以下格式：
Thought: [你的最终思考]
Final Answer: [最终答案]
"""
        return self.llm(prompt)
    
    def run(self, user_input: str, max_steps: int = 5) -> str:
        """运行ReAct循环"""
        for i in range(max_steps):
            # 思考
            thought = self.think(user_input)
            
            # 解析行动
            action, action_input = self._parse_action(thought)
            
            if action == "Final Answer":
                return self._extract_final_answer(thought)
            
            # 执行行动
            observation = self.act(action, action_input)
            
            # 记录
            self.memory.append((thought, action, observation))
        
        return "达到最大步数限制，未完成任务"
    
    def _format_memory(self) -> str:
        """格式化记忆"""
        if not self.memory:
            return "无"
        formatted = []
        for thought, action, observation in self.memory:
            formatted.append(f"Thought: {thought}\nAction: {action}\nObservation: {observation}")
        return "\n\n".join(formatted)
    
    def _parse_action(self, text: str) -> Tuple[str, str]:
        """解析行动"""
        lines = text.split("\n")
        action = None
        action_input = None
        for line in lines:
            if line.startswith("Action:"):
                action = line.split(":", 1)[1].strip()
            elif line.startswith("Action Input:"):
                action_input = line.split(":", 1)[1].strip()
        return action, action_input
    
    def _extract_final_answer(self, text: str) -> str:
        """提取最终答案"""
        if "Final Answer:" in text:
            return text.split("Final Answer:")[1].strip()
        return text
```

### 8.4 实验6.3：多Agent协作

```python
# multi_agent.py

class MultiAgentSystem:
    def __init__(self):
        self.agents = {}
        self.orchestrator = None
    
    def register_agent(self, name: str, agent):
        """注册Agent"""
        self.agents[name] = agent
    
    def set_orchestrator(self, orchestrator):
        """设置协调器"""
        self.orchestrator = orchestrator
    
    def delegate_task(self, task: str) -> str:
        """分配任务"""
        if self.orchestrator:
            plan = self.orchestrator.plan(task)
            results = []
            for step in plan:
                agent_name = step["agent"]
                sub_task = step["task"]
                result = self.agents[agent_name].run(sub_task)
                results.append({"agent": agent_name, "task": sub_task, "result": result})
            return self.orchestrator.synthesize(results)
        else:
            # 简单分配给第一个Agent
            return list(self.agents.values())[0].run(task)

class PlannerAgent:
    """规划Agent：将复杂任务分解为子任务"""
    def __init__(self, llm):
        self.llm = llm
    
    def plan(self, task: str) -> List[Dict]:
        prompt = f"""请将以下任务分解为子任务，并分配给合适的Agent。

任务：{task}

可用Agent：
- research_agent: 研究和信息收集
- coding_agent: 代码编写和调试
- writing_agent: 文档撰写

请用JSON格式返回计划：
[
    {{"agent": "agent_name", "task": "sub_task_description"}},
    ...
]
"""
        response = self.llm(prompt)
        return json.loads(response)
```

---

## 九、Phase 7：模型量化与部署（第8周）

### 9.1 目标

- 掌握模型量化技术
- 实现模型部署
- 优化推理性能

### 9.2 实验7.1：GGUF量化

```python
# quantization_example.py

# 使用llama.cpp进行量化（需要安装llama.cpp）

# 1. 下载原始模型
# wget https://huggingface.co/meta-llama/Meta-Llama-3-8B/resolve/main/consolidated.00.pth

# 2. 转换为GGUF格式
# python convert.py --outfile llama-3-8b-f16.gguf --outtype f16 ./meta-llama-3-8b/

# 3. 量化为不同精度
# ./llama-quantize llama-3-8b-f16.gguf llama-3-8b-q4_k_m.gguf Q4_K_M
# ./llama-quantize llama-3-8b-f16.gguf llama-3-8b-q5_k_m.gguf Q5_K_M
# ./llama-quantize llama-3-8b-f16.gguf llama-3-8b-q6_k.gguf Q6_K
# ./llama-quantize llama-3-8b-f16.gguf llama-3-8b-q8_0.gguf Q8_0

# 4. 推理测试
# ./llama-cli -m llama-3-8b-q4_k_m.gguf -p "What is LoRA?" -n 100

# Python API
import subprocess

def quantize_model(input_model, output_model, quant_type):
    """量化模型"""
    cmd = f"./llama-quantize {input_model} {output_model} {quant_type}"
    subprocess.run(cmd, shell=True, check=True)

def benchmark_quantized_model(model_path, prompt, n_tokens=100):
    """基准测试量化模型"""
    cmd = f"./llama-cli -m {model_path} -p '{prompt}' -n {n_tokens} --no-display-prompt"
    result = subprocess.run(cmd, shell=True, capture_output=True, text=True)
    return result.stdout
```

### 9.3 实验7.2：vLLM部署

```python
# vllm_deployment.py

# 安装vLLM: pip install vllm

from vllm import LLM, SamplingParams

# 1. 启动vLLM服务器
# python -m vllm.entrypoints.openai.api_server \
#     --model meta-llama/Meta-Llama-3-8B-Instruct \
#     --tensor-parallel-size 2 \
#     --max-model-len 4096

# 2. Python客户端
import requests

def query_vllm(prompt, max_tokens=512):
    """查询vLLM服务器"""
    response = requests.post(
        "http://localhost:8000/v1/completions",
        json={
            "model": "meta-llama/Meta-Llama-3-8B-Instruct",
            "prompt": prompt,
            "max_tokens": max_tokens,
            "temperature": 0.7,
        }
    )
    return response.json()

# 3. 直接使用Python API
llm = LLM(
    model="meta-llama/Meta-Llama-3-8B-Instruct",
    tensor_parallel_size=2,  # 使用2张GPU
    max_model_len=4096,
)

sampling_params = SamplingParams(
    temperature=0.7,
    top_p=0.9,
    max_tokens=512,
)

prompts = [
    "What is LoRA?",
    "Explain Flash Attention.",
    "How does MoE work?",
]

outputs = llm.generate(prompts, sampling_params)
for output in outputs:
    print(f"Prompt: {output.prompt}")
    print(f"Generated: {output.outputs[0].text}")
    print("---")
```

### 9.4 实验7.3：推测性解码

```python
# speculative_decoding.py

# 使用llama.cpp的推测性解码功能

# 1. 准备草稿模型（小模型）
# wget https://huggingface.co/meta-llama/Meta-Llama-3-8B/resolve/main/consolidated.00.pth
# python convert.py --outfile llama-3-8b-draft.gguf --outtype f16 ./llama-3-8b-draft/

# 2. 推测性解码推理
# ./llama-cli \
#     -m llama-3-8b-q5_k_m.gguf \
#     --model-draft llama-3-8b-draft.gguf \
#     --draft-prompt "What is LoRA?" \
#     -n 200

# 3. 性能对比
def benchmark_speculative_decoding():
    """对比推测性解码的性能"""
    prompts = [
        "What is LoRA?",
        "Explain Flash Attention.",
        "How does MoE work?",
    ]
    
    results = []
    for prompt in prompts:
        # 标准推理
        start = time.time()
        standard_output = benchmark_quantized_model("llama-3-8b-q5_k_m.gguf", prompt)
        standard_time = time.time() - start
        
        # 推测性解码（需要配置草稿模型）
        # speculative_output = benchmark_with_speculative_decoding(prompt)
        # speculative_time = ...
        
        results.append({
            "prompt": prompt,
            "standard_time": standard_time,
            # "speculative_time": speculative_time,
            # "speedup": standard_time / speculative_time
        })
    
    return results
```

---

## 十、Phase 8：端到端项目实战（第9-10周）

### 10.1 目标

- 完成一个完整的端到端项目
- 整合所有技术
- 形成可展示的作品

### 10.2 项目选择：智能文档问答系统

**项目描述**：基于RAG的智能文档问答系统，支持多文档类型、智能检索、答案生成

**技术栈**：
- 模型：Llama-3-8B-Instruct + LoRA
- 检索：向量检索 + 重排序
- 部署：vLLM + FastAPI
- 前端：Streamlit（可选）

**系统架构**：
```
用户输入
    ↓
查询理解（意图识别、查询扩展）
    ↓
检索阶段（向量检索 + BM25混合检索）
    ↓
重排序（Cross-Encoder）
    ↓
上下文构建（Top-K文档 + 提示词模板）
    ↓
答案生成（LLM推理）
    ↓
后处理（答案格式化、来源引用）
    ↓
返回结果
```

### 10.3 项目实现步骤

**Week 9：核心功能开发**

```python
# project_week9.py

# 1. 数据处理模块
class DocumentProcessor:
    def __init__(self):
        self.supported_formats = ["pdf", "docx", "txt", "md"]
    
    def process(self, file_path):
        """处理文档"""
        # 根据文件类型选择处理方法
        # 返回标准化的文档格式
        pass

# 2. 检索模块
class HybridRetriever:
    def __init__(self):
        self.vector_retriever = None
        self.bm25_retriever = None
        self.reranker = None
    
    def index(self, documents):
        """建立索引"""
        pass
    
    def retrieve(self, query, top_k=5):
        """混合检索"""
        pass

# 3. 生成模块
class AnswerGenerator:
    def __init__(self, llm, prompt_template):
        self.llm = llm
        self.prompt_template = prompt_template
    
    def generate(self, query, context):
        """生成答案"""
        pass
```

**Week 10：系统集成与优化**

```python
# project_week10.py

# 1. API服务
from fastapi import FastAPI, UploadFile
import uvicorn

app = FastAPI()

@app.post("/upload")
async def upload_document(file: UploadFile):
    """上传文档"""
    pass

@app.post("/query")
async def query_document(query: str):
    """查询文档"""
    pass

# 2. 性能优化
class PerformanceOptimizer:
    def __init__(self):
        self.cache = {}
        self.connection_pool = None
    
    def cache_query(self, query, result, ttl=3600):
        """缓存查询结果"""
        pass
    
    def batch_process(self, queries):
        """批量处理"""
        pass

# 3. 监控与日志
class Monitor:
    def __init__(self):
        self.metrics = {}
    
    def log_query(self, query, response, latency):
        """记录查询日志"""
        pass
    
    def get_metrics(self):
        """获取性能指标"""
        pass
```

### 10.4 项目展示准备

**README文档**：
- 项目简介
- 技术架构
- 安装部署
- 使用示例
- 性能指标

**演示视频**：
- 功能演示
- 技术亮点
- 性能对比

**代码仓库**：
- 清晰的代码结构
- 完整的文档
- 部署脚本

---

## 十一、学习路线与时间规划

### 11.1 总体时间规划（10周）

| 阶段 | 时间 | 内容 | 产出 |
|------|------|------|------|
| Phase 1 | 第1周 | 环境搭建 | 可用的开发环境 |
| Phase 2 | 第2周 | Transformer基础 | 理解核心原理 |
| Phase 3 | 第3周 | 注意力优化 | 掌握优化技术 |
| Phase 4 | 第4-5周 | 微调技术 | LoRA/QLoRA实践 |
| Phase 5 | 第6周 | RAG系统 | 检索增强生成 |
| Phase 6 | 第7周 | Agent系统 | 工具调用能力 |
| Phase 7 | 第8周 | 量化部署 | 部署能力 |
| Phase 8 | 第9-10周 | 端到端项目 | 完整作品 |

### 11.2 每周详细计划

**Week 1：环境搭建**
- Day 1-2：硬件环境检查、软件安装
- Day 3-4：项目结构创建、基础工具配置
- Day 5-7：环境验证、简单测试

**Week 2：Transformer基础**
- Day 1-2：阅读Transformer相关文章
- Day 3-4：实现简化版Transformer
- Day 5-6：注意力可视化、位置编码对比
- Day 7：实验记录、总结

**Week 3：注意力优化**
- Day 1-2：MHA/MQA/GQA实现
- Day 3-4：Flash Attention性能测试
- Day 5-6：KV Cache管理
- Day 7：性能对比、总结

**Week 4-5：微调技术**
- Week 4：LoRA微调7B模型
- Week 5：QLoRA微调70B模型、最佳实践验证

**Week 6：RAG系统**
- Day 1-3：基础RAG系统搭建
- Day 4-5：RAG优化（重排序、混合检索）
- Day 6-7：RAG评估、总结

**Week 7：Agent系统**
- Day 1-3：Function Calling实现
- Day 4-5：ReAct Agent实现
- Day 6-7：多Agent协作、总结

**Week 8：量化部署**
- Day 1-3：GGUF量化
- Day 4-5：vLLM部署
- Day 6-7：推测性解码、总结

**Week 9-10：端到端项目**
- Week 9：核心功能开发
- Week 10：系统集成、优化、展示准备

### 11.3 学习资源

**阅读材料**：
- 项目中的47篇技术文章
- 相关论文（LoRA、Flash Attention、RAG等）
- 技术博客和文档

**实践工具**：
- PyTorch、Transformers、PEFT
- vLLM、llama.cpp
- LangChain、ChromaDB

**社区资源**：
- HuggingFace模型和数据集
- GitHub开源项目
- 技术论坛和讨论组

---

## 十二、风险评估与应对策略

### 12.1 技术风险

| 风险 | 影响 | 概率 | 应对策略 |
|------|------|------|---------|
| PCIe通信瓶颈 | 多卡训练效率低 | 高 | 优先使用数据并行，避免频繁通信 |
| 显存不足 | 无法加载大模型 | 中 | 使用QLoRA，量化到4-bit |
| 模型效果不佳 | 微调后效果差 | 中 | 调整超参数，增加数据 |
| 依赖冲突 | 环境配置失败 | 低 | 使用conda隔离环境 |

### 12.2 时间风险

| 风险 | 影响 | 概率 | 应对策略 |
|------|------|------|---------|
| 学习曲线陡峭 | 进度延迟 | 中 | 优先核心内容，跳过次要细节 |
| 调试时间过长 | 项目延期 | 高 | 使用成熟框架，避免重复造轮子 |
| 硬件故障 | 实验中断 | 低 | 定期备份，保存检查点 |

### 12.3 资源优化策略

**GPU使用优化**：
1. **实验并行**：8卡同时跑8组不同实验
2. **模型选择**：优先使用7B-13B模型，节省显存
3. **量化优先**：能用量化就用量化，节省显存
4. **梯度累积**：小batch size + 梯度累积

**时间优化策略**：
1. **使用成熟框架**：避免重复造轮子
2. **从小开始**：先在小数据集上验证，再扩展到大数据集
3. **定期备份**：避免重复工作
4. **记录日志**：便于复现和调试

---

## 附录：8卡4090性能基准

### A.1 训练性能参考

| 任务 | 模型 | 配置 | 时间 | 显存 |
|------|------|------|------|------|
| LoRA微调 | Llama-3-8B | 单卡FP16 | 2小时 | 16GB |
| QLoRA微调 | Llama-3-70B | 单卡4-bit | 8小时 | 35GB |
| 全参数微调 | Llama-3-8B | 2卡DDP | 1小时 | 16GB/卡 |
| 全参数微调 | Llama-3-13B | 4卡DDP | 0.8小时 | 13GB/卡 |

### A.2 推理性能参考

| 模型 | 配置 | 吞吐量 | 延迟 |
|------|------|--------|------|
| Llama-3-8B | 单卡FP16 | 50 tokens/s | 20ms/token |
| Llama-3-8B | 单卡Q4_K_M | 70 tokens/s | 14ms/token |
| Llama-3-70B | 4卡FP16 | 20 tokens/s | 50ms/token |
| Llama-3-70B | 8卡Q4_K_M | 40 tokens/s | 25ms/token |

---

*本计划基于8卡RTX 4090配置，覆盖LLM全流程实践。*

*最后更新：2026年6月*